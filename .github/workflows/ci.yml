name: CI

on:
  pull_request:

jobs:
  isolated-api-runtime-skip:
    if: github.event.pull_request.head.ref != 'sandbox/api-runtime-v2-exec'
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Normal CI is disabled only on the isolated API runtime sandbox.'
