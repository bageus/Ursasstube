name: CI

on:
  pull_request:

jobs:
  disposable-diagnostic-skip:
    if: github.event.pull_request.head.ref != 'sandbox/api-reexport-static-exec'
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Normal CI is disabled only on the disposable diagnostic branch.'
