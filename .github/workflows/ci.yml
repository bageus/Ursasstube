name: CI

on:
  pull_request:

jobs:
  isolated-bootstrap-runtime-skip:
    if: github.event.pull_request.head.ref != 'sandbox/bootstrap-rank-runtime-exec'
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Normal CI is disabled only on the isolated bootstrap runtime sandbox.'
