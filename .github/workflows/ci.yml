name: CI

on:
  pull_request:

jobs:
  isolated-validation-skip:
    if: github.event.pull_request.head.ref != 'agent/static-export-cleanup'
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Normal CI is temporarily skipped while isolated validation runs.'
